<nav class="navbar navbar-expand-lg navbar-light">
  <%= link_to 'AccioCurios', root_path, class:"navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <%= link_to 'Browse Listings', listings_path, class:"nav-link" %>
      </li>
      <li class="nav-item">
      <%= link_to 'Create Listing', new_listing_path, class:"nav-link" %>
      </li>

    <% if user_signed_in? # If the user is signed then the sign out button appears %>
    <% if can? :manage, :all %>
    
      <%= link_to 'Admin Dashboard', admin_path, class:"nav-link" %>
    <% else %>
    <%= link_to 'My Dashboard', user_path, class:"nav-link" %>
    <% end %>
      <!-- User account dropdown -->
      <div class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <i class="fas fa-bars"></i>
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <%= link_to 'Edit Account', edit_user_registration_path, class:"dropdown-item" %>
          <div class="dropdown-divider"></div>
            <%= link_to 'Sign Out', destroy_user_session_path, :method => :delete , class:"dropdown-item" %> 
        </div>
      </div>
    <% else # User is not signed in %>
      <%= link_to 'Sign In', user_session_path, class:"btn btn-primary" %>
    <% end %>
    </ul>

    
    
  </div>
</nav>